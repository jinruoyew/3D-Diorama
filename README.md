# 3D-Diorama
Built a 3D model diorama of a room, showcased using a Unity build 
Build can be downloaded and run locally for showcase.

## How to Access the Unity Build
1. Download project.
2. Run the generated `.exe` file inside the build folder to launch the application.

